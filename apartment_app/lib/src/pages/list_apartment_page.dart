import 'package:flutter/material.dart';

class ListApartmentPage extends StatelessWidget {
  const ListApartmentPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("hello"),
      ),
    );
  }
}

